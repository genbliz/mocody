import { MyRepository } from "./repo";

function runInserts() {
  MyRepository.getIt()
    .then((d) => console.log(d))
    .catch((e) => console.log(e));
}

setTimeout(() => {
  console.log("Getting...");
  runInserts();
}, 10);

// npx tsx ./src/test/get-data.ts
